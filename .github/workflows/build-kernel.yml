name: Build DroidSpaces Kernel

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 180

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential bc bison flex libssl-dev libelf-dev gcc-aarch64-linux-gnu binutils-aarch64-linux-gnu git cpio zip unzip lz4 xz-utils device-tree-compiler libncurses-dev python3
          aarch64-linux-gnu-gcc --version | head -1

      - name: Clone kernel
        run: |
          git clone --depth=1 -b HyperOS1 https://github.com/Kuugo2002/xiaomi_kernel_nabu.git kernel
          head -5 kernel/Makefile
          ls kernel/arch/arm64/configs/ | head -20

      - name: Apply config
        run: |
          cd kernel
          # 用内核自带的 nabu defconfig 作为基底（4.14 源码自洽）
          BASE=$(ls arch/arm64/configs/ | grep -iE '^nabu.*defconfig$' | head -1)
          if [ -z "$BASE" ]; then
            BASE=$(ls arch/arm64/configs/ | grep -iE 'sm8150.*defconfig$' | head -1)
          fi
          echo "Base defconfig: $BASE"
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- "$BASE"

          # 只追加 DroidSpaces 需要的选项，不整份替换
          cat >> .config << 'EOF'
          CONFIG_NAMESPACES=y
          CONFIG_PID_NS=y
          CONFIG_UTS_NS=y
          CONFIG_IPC_NS=y
          CONFIG_USER_NS=y
          CONFIG_NET_NS=y
          CONFIG_SYSVIPC=y
          CONFIG_POSIX_MQUEUE=y
          CONFIG_CGROUPS=y
          CONFIG_CGROUP_DEVICE=y
          CONFIG_CGROUP_PIDS=y
          CONFIG_CGROUP_FREEZER=y
          CONFIG_CGROUP_SCHED=y
          CONFIG_FAIR_GROUP_SCHED=y
          CONFIG_MEMCG=y
          CONFIG_SECCOMP=y
          CONFIG_SECCOMP_FILTER=y
          CONFIG_DEVTMPFS=y
          CONFIG_OVERLAY_FS=y
          CONFIG_TMPFS_POSIX_ACL=y
          CONFIG_TMPFS_XATTR=y
          CONFIG_VETH=y
          CONFIG_BRIDGE=y
          CONFIG_BRIDGE_NETFILTER=y
          CONFIG_NETFILTER_ADVANCED=y
          CONFIG_NF_CONNTRACK=y
          CONFIG_NF_NAT=y
          CONFIG_IP_NF_IPTABLES=y
          CONFIG_IP_NF_FILTER=y
          CONFIG_IP_NF_TARGET_MASQUERADE=y
          CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y
          # CONFIG_ANDROID_PARANOID_NETWORK is not set
          EOF
          sed -i 's/^          //' .config

          # 让 Kconfig 解析依赖，自动处理冲突项
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- olddefconfig

          echo "=== DroidSpaces config verification ==="
          for c in CONFIG_NAMESPACES CONFIG_PID_NS CONFIG_UTS_NS CONFIG_IPC_NS \
                   CONFIG_CGROUPS CONFIG_CGROUP_DEVICE CONFIG_DEVTMPFS \
                   CONFIG_OVERLAY_FS CONFIG_SECCOMP CONFIG_VETH CONFIG_BRIDGE; do
            if grep -q "^${c}=y" .config; then echo "OK   ${c}"; else echo "MISS ${c}"; fi
          done

      - name: Patch tracepoint include path
        run: |
          cd kernel
          # drivers/hid/hid-trace.c 通过 define_trace.h 反向 include hid-trace.h，
          # 但 4.14 的 drivers/hid/Makefile 没给该目标加 -I$(src)，
          # 导致 TRACE_INCLUDE 解析失败。补上标准修法。
          if [ -f drivers/hid/Makefile ] && ! grep -q 'CFLAGS_hid-trace.o' drivers/hid/Makefile; then
            printf '\nCFLAGS_hid-trace.o += -I$(src)\n' >> drivers/hid/Makefile
            echo "Patched drivers/hid/Makefile:"
            tail -3 drivers/hid/Makefile
          fi

      - name: Build kernel
        run: |
          cd kernel
          # 4.14 源码用现代 GCC 13 编译会触发大量新增警告，
          # 而内核 Makefile 带 -Werror，需要放宽为非致命。
          KCFLAGS="-Wno-error \
            -Wno-error=misleading-indentation \
            -Wno-error=designated-init \
            -Wno-error=array-bounds \
            -Wno-error=stringop-overflow \
            -Wno-error=stringop-truncation \
            -Wno-error=maybe-uninitialized \
            -Wno-error=dangling-pointer \
            -Wno-error=enum-conversion \
            -Wno-error=address \
            -Wno-error=unused-variable \
            -Wno-error=unused-but-set-variable \
            -Wno-error=implicit-fallthrough \
            -Wno-error=packed-not-aligned \
            -Wno-error=attributes \
            -Wno-error=format-overflow \
            -Wno-error=format-truncation \
            -Wno-error=restrict \
            -Wno-error=nonnull \
            -Wno-error=memset-elt-size \
            -Wno-error=sizeof-pointer-memaccess \
            -Wno-error=pointer-sign \
            -Wno-error=incompatible-pointer-types \
            -Wno-error=int-conversion \
            -Wno-error=discarded-qualifiers \
            -Wno-error=return-type"

          set -o pipefail
          make -j$(nproc) \
            ARCH=arm64 \
            CROSS_COMPILE=aarch64-linux-gnu- \
            KCFLAGS="$KCFLAGS" \
            CONFIG_WERROR=n \
            Image modules dtbs 2>&1 | tee ../build.log || {
              echo "=== BUILD FAILED: first errors ==="
              grep -iE "error:|fatal error:" ../build.log | head -20
              exit 1
            }

          echo "=== Build result ==="
          ls -lh arch/arm64/boot/Image

      - name: Package
        run: |
          mkdir -p out release
          cp kernel/arch/arm64/boot/Image out/
          find kernel/arch/arm64/boot/dts -name "*nabu*.dtb" -exec cp {} out/ \; 2>/dev/null || true
          find kernel/arch/arm64/boot/dts -name "*sm8150*.dtb" -exec cp {} out/ \; 2>/dev/null || true
          cd kernel
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- INSTALL_MOD_PATH=../out/modules modules_install || true
          cd ..
          cp out/Image release/
          cp out/*.dtb release/ 2>/dev/null || true
          cd release
          zip -r ../DroidSpaces_nabu.zip .
          cd ..
          ls -lh DroidSpaces_nabu.zip

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: droidspaces-kernel-nabu
          path: |
            out/
            release/
            DroidSpaces_nabu.zip
            build.log
          retention-days: 30
