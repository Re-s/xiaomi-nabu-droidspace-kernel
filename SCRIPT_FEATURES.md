# build_kernel.sh 脚本特性总结

## 🎯 主要特性

`build_kernel.sh` 是一个生产级的内核编译脚本，为小米平板5 (nabu) 设备提供完整的内核构建和 boot.img 生成解决方案。

### 1. 智能依赖管理
- **自动检测系统类型**: Ubuntu/Debian/CentOS/Fedora/Arch Linux
- **自动安装依赖**: 检测缺失的包并尝试安装
- **权限处理**: 优雅处理无 root 权限的情况，提供手动安装指南
- **交叉编译器验证**: 自动检测和安装 aarch64-linux-gnu-gcc

### 2. 内核源码管理
- **智能克隆**: 支持代理检测（proxychains），自动回退到直接克隆
- **版本验证**: 自动验证内核版本是否匹配
- **更新支持**: 支持 `--update` 参数更新内核源码
- **深度克隆**: 使用 `--depth=1` 减少克隆时间和空间

### 3. 配置管理
- **自动复制 defconfig**: 从项目目录复制到内核源码
- **配置验证**: 自动验证 DroidSpaces 关键配置项
- **智能配置**: 运行 `olddefconfig` 解决新选项
- **详细报告**: 显示所有必需和推荐配置项的状态

### 4. 并行编译
- **自动线程检测**: 默认使用所有可用 CPU 核心
- **自定义线程数**: 支持 `-t` 参数指定编译线程数
- **编译时间记录**: 显示总编译时间
- **错误处理**: 编译失败时显示详细错误信息

### 5. 多种打包方式

#### AnyKernel3 ZIP（推荐）
- **Recovery 刷入**: 适用于 TWRP/OrangeFox Recovery
- **模块包含**: 自动包含内核模块
- **DTB 支持**: 包含设备树文件
- **完整配置**: 包含所有刷入脚本

#### mkbootimg 生成
- **Fastboot 刷入**: 直接生成 boot.img 用于 fastboot
- **高通参数**: 预配置 SM8150 启动参数
- **最小化 ramdisk**: 创建最小 ramdisk 用于启动
- **完整文档**: 提供详细的刷入说明

### 6. 完整发布包
- **多格式输出**: ZIP、tar.gz、boot.img
- **MD5 校验**: 自动生成校验和
- **刷入脚本**: 包含自动化刷入脚本
- **详细文档**: 包含 README 和使用说明

### 7. 错误处理和日志
- **完整日志**: 记录所有构建步骤
- **错误捕获**: 使用 `set -e` 和 trap 捕获错误
- **详细报告**: 显示错误位置和相关日志
- **清理函数**: 错误时自动清理临时文件

### 8. 命令行界面
- **丰富的选项**: 支持多种构建模式
- **帮助系统**: 详细的帮助信息和示例
- **灵活控制**: 支持部分构建和打包
- **更新支持**: 支持更新内核源码

## 🚀 使用示例

### 完整构建
```bash
./build_kernel.sh
```

### 增量编译
```bash
./build_kernel.sh -n
```

### 仅生成 AnyKernel3 包
```bash
./build_kernel.sh --use-anykernel
```

### 仅生成 boot.img
```bash
./build_kernel.sh --use-mkbootimg
```

### 清理构建
```bash
./build_kernel.sh -c
```

## 📊 性能优化

### 编译时间
- **首次编译**: 15-45分钟（取决于CPU核心数）
- **增量编译**: 3-10分钟
- **模块编译**: 2-5分钟

### 磁盘空间
- **内核源码**: ~2GB
- **编译输出**: ~5GB
- **发布包**: ~500MB

### 内存使用
- **推荐**: 16GB+
- **最低**: 8GB（使用较少线程）

## 🔧 高级配置

### 自定义线程数
```bash
./build_kernel.sh -t 8  # 使用8个线程
```

### 环境变量
```bash
export ARCH=arm64
export CROSS_COMPILE=aarch64-linux-gnu-
./build_kernel.sh
```

### 仅设置环境
```bash
./build_kernel.sh -s
```

### 仅编译内核
```bash
./build_kernel.sh -k
```

## 🛡️ 安全特性

1. **权限检查**: 检测 root 权限并提供建议
2. **依赖验证**: 验证所有必需工具
3. **配置验证**: 验证关键内核配置
4. **错误处理**: 优雅处理各种错误情况
5. **日志记录**: 完整记录所有操作

## 📈 与基础脚本的对比

| 特性 | build_kernel.sh | build.sh |
|------|----------------|----------|
| 依赖管理 | ✅ 自动检测和安装 | ❌ 手动安装 |
| 内核源码管理 | ✅ 智能克隆和更新 | ❌ 基本克隆 |
| 配置验证 | ✅ 详细验证 | ⚠️ 基本验证 |
| 打包方式 | ✅ AnyKernel3 + mkbootimg | ⚠️ 仅AnyKernel3 |
| 错误处理 | ✅ 完整错误处理 | ⚠️ 基本错误处理 |
| 日志记录 | ✅ 详细日志 | ⚠️ 基本日志 |
| 命令行选项 | ✅ 丰富选项 | ⚠️ 基本选项 |
| 文档 | ✅ 完整文档 | ⚠️ 基本文档 |

## 🎯 适用场景

1. **生产环境**: 适合正式发布内核
2. **开发环境**: 支持增量编译和调试
3. **CI/CD**: 集成到自动化构建流程
4. **多平台**: 支持多种 Linux 发行版
5. **新手友好**: 自动处理复杂配置

## 📝 注意事项

1. **首次编译**需要较长时间
2. **确保足够的磁盘空间**（至少50GB）
3. **建议使用 SSD** 加快编译速度
4. **编译时关闭不必要的程序**
5. **备份原始 boot.img** 以防刷机失败

## 🆘 故障排除

### 常见问题
1. **依赖缺失**: 使用 `--setup-only` 检查
2. **编译失败**: 检查 `logs/kernel_compile.log`
3. **boot.img 生成失败**: 使用 `--use-anykernel` 替代
4. **权限问题**: 手动安装依赖

### 获取帮助
```bash
./build_kernel.sh -h  # 查看帮助
cat logs/build.log    # 查看构建日志
./verify_kernel.sh    # 验证内核配置
```

## 🎉 总结

`build_kernel.sh` 是一个功能完整、易于使用的生产级内核编译脚本。它提供了从依赖管理到最终发布的完整解决方案，适合各种使用场景。无论你是新手还是专家，都能轻松使用它来构建和发布内核。